<div class="container">
     <div class="row">
        <div class="col-md-5 col-sm-10 mx-2 mx-sm-auto rounded px-0 my-5">
            <h3>マイページ</h3>
            <p>登録情報 
            <%= link_to edit_customer_path(current_customer.id), class: "btn btn-success" do %>
            編集する
            <% end %>
            </p>
            <table class="table table table-bordered">
                <tbody>
                    <tr>
                        <th scope="row">氏名</th>
                        <td><%= @customer.last_name %>　<%= @customer.first_name %></td>
                    </tr>
                    <tr>
                        <th scope="row">カナ</th>
                        <td><%= @customer.last_name_kana %>　<%= @customer.first_name_kana %></td>
                    </tr>
                    <tr>
                        <th scope="row">郵便番号</th>
                        <td><%= @customer.postal_code %></td>
                    </tr>
                    <tr>
                        <th scope="row">住所</th>
                        <td><%= @customer.address %></td>
                    </tr>
                    <tr>
                        <th scope="row">電話番号</th>
                        <td><%= @customer.telephone_number %></td>
                    </tr>
                    <tr>
                        <th scope="row">メールアドレス</th>
                        <td><%= @customer.email %></td>
                    </tr>
                </tbody>
            </table>
            <p>配送先　
            <%= link_to addresses_path, class: "btn btn-primary" do %>
            一覧を見る
            <% end %>
            </p>
            <p>注文履歴
            <%= link_to orders_path, class: "btn btn-primary" do %>
            一覧を見る
            <% end %>
            </p>
        </div>
    </div>
</div>